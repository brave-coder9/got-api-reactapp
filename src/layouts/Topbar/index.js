/* eslint-disable react-hooks/exhaustive-deps */
import { TopbarContainer } from "./style";

function Topbar() {
  return (
    <TopbarContainer>
      Navbar
    </TopbarContainer>
  );
}

export default Topbar;
